generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../sqlite.db"
}

model UserConv {
  id           Int     @id @default(autoincrement())
  userId       String?
  conversionId String?

  @@index([userId], map: "idx_UserConv")
}
